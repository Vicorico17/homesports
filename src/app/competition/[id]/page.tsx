import Link from "next/link";
import { getMatches } from "@/lib/matches";
type Standing = { rank?: number; team?: { id?: number; name?: string; image_url?: string | null }; wins?: number; losses?: number; points?: number; score?: number };
export const revalidate = 60;
async function panda<T>(path: string, token: string) { const response = await fetch(`https://api.pandascore.co/${path}`, { headers: { Authorization: `Bearer ${token}` }, next: { revalidate: 60 } }); return response.ok ? await response.json() as T : []; }
export default async function CompetitionPage({ params }: { params: Promise<{ id: string }> }) {
  const { id } = await params; const { matches } = await getMatches(); const competitionMatches = matches.filter((match) => String(match.tournamentId) === id); const source = competitionMatches[0]; const token = process.env.PANDASCORE_API_KEY; const standings = token ? await panda<Standing[]>(`tournaments/${id}/standings?per_page=100`, token) : [];
  const rows: Standing[] = standings.length ? standings : [...new Map(competitionMatches.flatMap((match) => match.opponents.map((team) => [team.id, team]))).values()].map((team, index) => ({ rank: index + 1, team: { id: team.id, name: team.name, image_url: team.imageUrl } }));
  return <main className="competition-page"><Link href="/competitions">← All competitions</Link><p className="eyebrow">{source?.league ?? "COMPETITION"}</p><h1>{source?.tournament || source?.serie || "Competition"}</h1><p className="page-intro">Deep results, tournament standings, and the road ahead.</p><section className="competition-section"><h2>Leaderboard</h2>{rows.length ? <div className="standings"><div className="standing-row heading"><span>#</span><span>TEAM</span><span>W–L</span><span>POINTS</span><span>FORM</span></div>{rows.map((row, index) => { const teamMatches = competitionMatches.filter((match) => match.opponents.some((team) => team.id === row.team?.id)); const form = teamMatches.filter((match) => match.status === "finished").sort((a, b) => new Date(b.beginAt).getTime() - new Date(a.beginAt).getTime()).slice(0, 5).map((match) => { const current = match.opponents.find((team) => team.id === row.team?.id); const other = match.opponents.find((team) => team.id !== row.team?.id); return current && other && (current.score ?? 0) > (other.score ?? 0) ? "W" : "L"; }); return <div className="standing-row" key={row.team?.id ?? row.team?.name ?? index}><span>{row.rank ?? index + 1}</span><span className="standing-team">{row.team?.image_url ? <img src={row.team.image_url} alt="" /> : <i>{row.team?.name?.slice(0, 1)}</i>}<a href={`/teams/${row.team?.id}`}>{row.team?.name ?? "Unknown team"}</a></span><b>{row.wins ?? "—"}–{row.losses ?? "—"}</b><span>{row.points ?? row.score ?? "—"}</span><span className="form">{form.map((result, resultIndex) => <i className={result === "W" ? "win" : "loss"} key={resultIndex}>{result}</i>)}</span></div>; })}</div> : <p className="empty">No standings are available for this competition yet.</p>}</section><section className="competition-section"><h2>Recent results</h2><div className="history-list">{competitionMatches.filter((match) => match.status === "finished").map((match) => { const high = Math.max(...match.opponents.map((team) => team.score ?? -1)); return <div className="history-row" key={match.id}><small>{new Date(match.beginAt).toLocaleDateString(undefined, { month: "short", day: "numeric" })}</small><span>{match.opponents.map((team) => <a className={team.score === high ? "winner" : ""} href={`/teams/${team.id}`} key={team.id}>{team.name}</a>)}<small className="competition-label">{match.leagueImageUrl ? <img src={match.leagueImageUrl} alt="" /> : null}{match.league}</small></span><b>{match.opponents.map((team) => <span className={team.score === high ? "winner" : ""} key={team.id}>{team.score ?? "—"}</span>)}</b></div>; })}</div></section><section className="competition-section"><h2>Upcoming</h2><div className="history-list">{competitionMatches.filter((match) => match.status === "upcoming" || match.status === "running").map((match) => <div className="history-row" key={match.id}><small>{match.status === "running" ? "LIVE" : new Date(match.beginAt).toLocaleDateString(undefined, { month: "short", day: "numeric" })}</small><span>{match.opponents.map((team) => team.name).join(" vs ")}</span><b>{match.status === "running" ? "NOW" : "SOON"}</b></div>)}</div></section></main>;
}
